import 'package:flutter/material.dart';

class NewsGalleryScreen extends StatelessWidget {
  const NewsGalleryScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('News & Gallery')), body: const Center(child: Text('News feed will appear here.')));
}
